export interface MovieCard{
        movieId: number;
        revenue: any;
        genreId: number;
        genreName?: any;
        movieTitle: string;
        moviePosterUrl: string;
        pages: number;
        rating: number;
    }

// vs code shortcut
// Ctrl+Shift+p => open all the settings in VS code
// Ctrl+p =>
// Alt+shift+f => formatting your code
// Alt+shift+Arrow Down => paste the current line below
// Alt+O