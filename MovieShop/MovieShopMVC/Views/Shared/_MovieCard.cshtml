@model ApplicationCore.Models.MovieCardResponseModel

    <div class="card mb-4 shadow">
        <a asp-controller="Movies" asp-action="Details" asp-route-id="@Model.MovieId">
            <img src="@Model.MoviePosterUrl" class="img-fluid" alt="@Model.MovieId" />
        </a>
    </div>

